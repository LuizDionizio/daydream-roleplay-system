/**
 * Responsável pelos tipos espaciais do Board.
 * Define estruturas de entidades renderizadas na viewport.
 */

export type BoardTokenData = {
  id: string;
  x: number;
  y: number;
  name: string;
};
